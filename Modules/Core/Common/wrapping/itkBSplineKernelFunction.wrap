itk_wrap_class("itk::BSplineKernelFunction" POINTER)
  foreach(r ${WRAP_ITK_REAL})
    foreach(d ${ITK_WRAP_IMAGE_DIMS})
      itk_wrap_template("${d}${ITKM_${r}}" "${d},${ITKT_${r}}")
    endforeach()
  endforeach()
itk_end_wrap_class()